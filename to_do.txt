v - Добавить в Card & Imitate() разновидности айдишников
v - Убедиться, что все написанные imitate работают корректно
v - Запустить imitate в цикле для разных значений параметров и сколлекционировать все несработавшие параметры

v - В тесте добавить рассчет status в expected
v - Протестировать на разных значениях
V - ввести successful в карточку
V - вывести имя и значения карточки
v - протестировать классы Card и Success
V - ветка и класс error
V - сделали класс Success для инита карточки

=============

v - выводить ошибки в ЛОГ
v - Оттестить Логгера

=============

v - перевести карточку на режим значений в подсписках
v - Оттестить

=============

v - is_selflink_value
v - is_selflink_template_value
v - is_id_template_value
v - is_id_selflink_value
v - is_id_selflink_template_value

Осталось:
v - is_usual_value (метод исключения, в том числе из id)
v - is_id_value (нужен Парсер...)
v - is_template_value (серчит по ссылкам, которые УЖЕ должны быть разложенны по json'у...

! - Оттестить!

=============

! - Делывать КардМенеджера (по ТЗ из Парадигм_Зеро.txt в главной ветке)
v - Пропарсить json-value и выставить типы значений пользовательскими заглушками 
     (потом настроим автоматику Парсера для id-val)
~ - заполнить все всп. поля - ПОКА ЧТО ЗАГЛУШКА:
х - -кард менеджер должен определить ссылки
~ - должен определить типы значений (нужен Парсер)
x - проверить циклы

V - разобраться с ворнингами (и прочем) в каждом классе
v - перетащить ввод из Zero в main.py
? - что делать сперва: определять тип значения ("мигрирующей лексемы") или заполнять поле ссылок данного значения?
v - добавить сообщение о том, что Карточка обрубила значение
V - Переименовать notification -> ParentNotification и создать общий Notification с объектами
  - -Ошибки (мешают работе программы)
    -Ворнинги (мешают, но не фатально)
    -Сообщения (не мешают - а лишь информируют об уже сделанном)
    -Заметки (информируют об альтернативных методах, как можно было сделать)
V - Добавил к ошибкам информацию:
    -Уровень в стеке вызовов
    -файл
    -строка
    -координата в строке 
    -фунцкция/объект
    -сама строка (участок строки, из которого идет вызов и из которой подтягивается ошибка)
    -Текст ошибки

    //Все классы структуры Зеро выбрасывают наверх ТОЛЬКО ДЖЕЙСОН. Текст оформляет MAIN.

~ - Сделать то же для Карточки?

! - ДИМЕ: В ОТДЕЛЬНОЙ ВЕТКЕ!
! - ДИМЕ: Исправить количество correct/incorrect (all_vals штук - СЕЙЧАС их МЕНЬШЕ) в imitate & Card
! - ДИМЕ: Исправить Card == imitate (чтобы DANGER_ZONE не выдавал ошибок)

x - Собрать весь вывод в отдельный класс. Подсмотреть в Solved puzzles over-classes amount

x - Разделить пользовательские и разработческие ошибки в два разных объекта Логгера 
    (проносить их сквозь все уровни)

V - Проблема: логи не совпадают. Искусственный(fail_log, success_log) и генерящийся сейчас (ZeroLog.json)
  не совпадает зона cards
  а именно: типы значений хранят списки вместо json, как в искусственном.
  сделать json.
  //Мы сделали тестовый json в CardManager.gen_value_links_info()

x - Починить рекурсию в замыкании CardManager.value_validate() -> __inner()